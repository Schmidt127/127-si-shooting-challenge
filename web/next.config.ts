import type { NextConfig } from "next";

/**
 * Next.js configuration for the 127 SI Shooting Challenge web app.
 * Add image domains, redirects, and headers here as public pages ship.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
