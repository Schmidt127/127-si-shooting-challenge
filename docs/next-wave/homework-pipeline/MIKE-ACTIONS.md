# Agent 11 — Mike actions

1. **UI-attest PROD deletes** for **063** and **111** (name / ON-OFF / version list). Do not reinstall 111 if **013 v2.0** is live.
2. **Decide quiz path** for HW17: recommended **Option B** (attachment-less). If you want Drive/file parity, choose **Option A** and authorize OMNI to add `Quiz Result PDF` + Fillout mapping (see `QUIZ-PATH-DECISION.md`).
3. **Orphan blank Grade Band HCs:** after wipe / historical rows, either re-touch assets through 020 or authorize a one-time blank-GB repair script — do **not** blindly restore full 063.
4. **Learning Activities schema:** approve DEV table creation only after homework file + quiz paths are live-proven (`LEARNING-ACTIVITIES-SCHEMA.md`). No competing XP writers.
5. **Agent 14:** apply `STALE-063-111-PATCH-MANIFEST.md` to shared docs/audits after concurrent agents finish.
6. **Optional follow-up (Cursor):** remove dead `CONFIG.submissions.gradeBand` prefer-path from 020 in a dedicated DEV change (soft-fails today; documents a false field).
