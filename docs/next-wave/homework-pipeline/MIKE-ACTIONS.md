# Agent 11 — Mike actions

1. **UI-attest PROD deletes** for **063** and **111** (name / ON-OFF / version list). Do not reinstall 111 if **013 v2.0** is live.
2. ~~**Decide quiz path** for HW17~~ — **DECIDED Option B** (attachment-less). See `QUIZ-PATH-DECISION.md`.
3. **Paste / confirm PROD 067 Option B** using [`067-OPTION-B-PROD-INSTALL.md`](./067-OPTION-B-PROD-INSTALL.md). Do **not** create `Quiz Result PDF`. Run Schmidt T1–T2 live proof when API/UI available.
4. **Orphan blank Grade Band HCs:** after wipe / historical rows, either re-touch assets through 020 or authorize a one-time blank-GB repair script — do **not** blindly restore full 063.
5. **Learning Activities schema:** approve table creation only after homework file + quiz paths are live-proven (`LEARNING-ACTIVITIES-SCHEMA.md`). No competing XP writers.
6. **Agent 14:** apply `STALE-063-111-PATCH-MANIFEST.md` to shared docs/audits after concurrent agents finish.
7. **Optional follow-up (Cursor):** remove dead `CONFIG.submissions.gradeBand` prefer-path from 020 in a dedicated change (soft-fails today; documents a false field).
8. **Cloud agent blocker:** add `AIRTABLE_API_TOKEN` + `AIRTABLE_BASE_ID=appn84sqPw03zEbTT` to Cursor environment so Schmidt live packs can run without UI-only steps (`docs/prod-completion/2026-07-25/ACCESS-BLOCKER.md`).
