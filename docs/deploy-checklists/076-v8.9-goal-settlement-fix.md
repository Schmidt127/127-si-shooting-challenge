# 076 v8.9 — goal settlement fix (057 v1.9 alignment)

Status: **Repository-ready / promotion pending**
Production change: **Not applied by Cursor**

## Problem

v8.8 compared WAS **Weekly Goal Shots Target** (formula `Goal Shots Target / 9`,
e.g. `1111.11…`) to Goal Record **Total Shot Target** (season total, e.g.
`10000`). Settled Production weeks fail-closed with:

> Weekly goal configuration is missing, ambiguous, wrong-scope, inactive, or unsettled

Example: Curtis submission `recwofzVvYsAYMibR` / WAS `reckHM0Hio7fyp9FF`.

## Fix (v8.9)

Align with **057 v1.9**:

| Purpose | WAS field | Goal Record field |
|---|---|---|
| **Settlement** | `Goal Shots Target` (season lookup) | `Total Shot Target` |
| **Weekly math** | `Weekly Goal Shots Target` (`Goal/9`) | — |

## Related script

**058 v1.4** receives the same settlement correction for Perfect Week unlock
validation.

## Production paste

1. Open Production Automation **076 - Daily Submission Communications Hub Handoff**.
2. Paste committed **v8.9** source (docblock through end; skip GitHub header).
3. Re-run Curtis submission `recwofzVvYsAYMibR` through 031 → 076.
4. Confirm queue handoff creates with payload including `weekDateRange`,
   `homeworkItems`, and `xpPageUrl`.

Optional same-session paste: **058 v1.4** if Perfect Week unlock validation is
also failing on settled 10,000 / 1,111… weeks.

## Validation

```bash
node --test tests/email/automation-076-offline.test.mjs
node tools/testing/tests/test_076_email_handoff_runtime.mjs
node tools/testing/tests/test_058_perfect_week_lifecycle_runtime.mjs
```
