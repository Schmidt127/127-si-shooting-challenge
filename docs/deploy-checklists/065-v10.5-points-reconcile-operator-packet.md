# Operator packet — Automation 065 v10.5 (XP points reconcile fix)

**Status:** **COMPLETE** — Mike pasted **v10.5** to Production 2026-08-31; Parent Feedback Sent? / Sent On verified on run  
**Paste still required?** **No**  
**Date:** 2026-08-31  
**Incident:** Partial Extra Credit XP (e.g. 4 pts) left Award Status Pending; later Base XP total (e.g. 39) could not reconcile because ownership assert required points to already match. Blocked **071** parent feedback until fix.

| Item | Value |
|------|--------|
| Automation | **065 - Homework Review and XP - Create or Reconcile Homework XP Event** |
| Automations table record | `recIiu9B03aL3NVDC` |
| Automation workflow ID | `wfllkhzl3R6OlClzy` |
| Repo / intended | **v10.5** |
| Production Live | **v10.5** (Mike paste + Sent?/Sent On verify 2026-08-31) |
| Script | `airtable/automations/shooting-challenge/065-homework-review-and-xp-create-homework-xp-event.js` |
| **Copy/paste source** | [`065-v10.5-PASTE.txt`](./065-v10.5-PASTE.txt) — production docblock → EOF (skip GitHub header) |

---

## What changed (v10.5)

`assertOwned` no longer fails when `XP Points !== Total Homework XP Awarded`.

Ownership still requires matching Source Key, Homework Completion, Enrollment, Week, and Submission. Step 7 still **updates** points on the existing `HOMEWORK_XP|{hcId}` event and sets **Award Status = Awarded**.

This fixes the Extra Credit → Base XP race: first reconcile may create a low-point event; when totals rise, 065 updates the same row instead of erroring.

---

## Contract snapshot (unchanged)

| Item | Value |
|------|--------|
| Trigger | When `Homework XP Reconciliation Needed? = 1` |
| Table | Homework Completions |
| Dynamic `recordId` | Triggering Homework Completion Record ID (**keep** — do not hardcode) |
| Source Key | `HOMEWORK_XP\|{homeworkCompletionId}` |
| Dependencies | **064** prepares totals; **020** creates HC; **078** → **071** after Awarded |

---

## Paste steps

1. Open Production automation **065** (`wfllkhzl3R6OlClzy`).
2. Keep automation **ON** if possible; otherwise pause only while pasting.
3. Open the script action. Confirm input **`recordId`** is still dynamic (`$ref` → trigger `id`) — **do not** paste a literal `rec…`.
4. Replace the script body with [`065-v10.5-PASTE.txt`](./065-v10.5-PASTE.txt) (entire file).
5. Confirm SCRIPT metadata shows **version: v10.5** / **lastUpdated: 2026-08-31**.
6. Save / finish editing. Update Automations Code tracker text to **v10.5** if you use that column.
7. Optional: update repo note in CHANGELOG that Production paste is done after you paste.

---

## Verify (Schmidt disposable)

1. Prefer a disposable HC where Extra Credit is set first, then Base XP (or bump Total after a partial award).
2. Confirm `Homework XP Reconciliation Needed?` enters **1**.
3. Expect: one `HOMEWORK_XP|{hcId}` row; **XP Points** = Total Homework XP Awarded; **Award Status = Awarded**; Reconcile returns **0**.
4. Then **071** can hand off when Ready / Satisfactory / Coach Feedback gates are met.

**Do not** re-test on the already-repaired Meditation Workout HC (`recIuZQ6f2tk3bnIr`) unless intentionally re-entering reconcile.

---

## Stop / rollback

- Stop if `recordId` input is not dynamic after save.
- Rollback: restore prior v10.4 script from git (`git show <sha>:airtable/automations/shooting-challenge/065-homework-review-and-xp-create-homework-xp-event.js`) and re-paste.

---

## Offline proof (repo)

```powershell
node --test tools/testing/tests/test_065_066_trigger_record.mjs
node tests/automation-contracts/065-066-trigger-record.test.js
```

Includes: **065 reconciles XP points when Total Homework XP changes after partial award**.
