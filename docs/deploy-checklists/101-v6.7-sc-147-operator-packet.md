# Operator packet — Automation 101 v6.7 (SC-147 recording half-XP)

**Backlog:** SC-147 / MRW-H10  
**Script:** `airtable/automations/shooting-challenge/101-zoom-attendance-xp-award-meeting-xp.js`  
**Version:** v6.7  
**Status:** GitHub ready — **NOT pasted to Production**

---

## Summary

Extends existing **101 - Zoom Attendance XP - Award Meeting XP** with an SC-147 recording half-XP phase in the same meeting reconciliation run. **No new automation slot.**

| Path | XP | Source Key prefix |
|------|-----|-------------------|
| Live (unchanged) | Full (`ZOOM_ATTEND_BASE`) | `ZOOM_ATTEND_BASE\|{Zoom Meeting Key}\|{Enrollment}` |
| Recorded (new in v6.7) | Half (`ZOOM_RECORDING` or floor(live/2)) | `ZOOM_RECORDING_CREDIT\|{Enrollment}\|{Zoom Meeting}` |

**117 remains email-only** — do not add XP writes to 117.

---

## Preconditions (Mike / OMNI)

1. Optional **`ZOOM_RECORDING`** row in XP Reward Rules (fallback: floor(live/2))
2. `Zoom XP Reconciliation Needed?` should flip when recording credit is pending (formula review in OMNI)
3. Disposable DEV enrollment for proof (high-autonomy mode authorized)

---

## DEV install steps

1. Open Automation **101** in DEV base
2. Paste from GitHub: docblock through end of `101-zoom-attendance-xp-award-meeting-xp.js` (skip GitHub header)
3. Confirm trigger unchanged: **Zoom Meetings** · `Zoom XP Reconciliation Needed? = 1`
4. Confirm input: `recordId` from triggering record

---

## Disposable proof matrix

| # | Scenario | Expected |
|---|----------|----------|
| 1 | Live attendee, Create XP Events armed | Full live XP; no recording credit |
| 2 | Recording quiz approved, not on Attendees | Half XP; `ZOOM_RECORDING_CREDIT\|*` |
| 3 | Quiz not approved | No recording XP |
| 4 | Rerun same recording | Skipped (idempotent) |
| 5 | Live + recording same meeting | Live only; recording skipped or deactivated |
| 6 | Two different meetings, same athlete | Each can have its own credit type |
| 7 | Conflict rollup = 1 | Recording skipped |

---

## Production promotion (after DEV proof)

1. Mike approval
2. Paste **101 v6.7** to Production (replace script body only)
3. Update `CHANGELOG.md` under `### Airtable`
4. Re-run SC-087 exclusivity spot-check

**Do not** create Automation 121.
