# Deploy checklist — 072 v4.7 weekly email fixes

**Date:** 2026-08-24  
**GitHub:** `072` v4.7, `074` v3.3  
**Test WAS:** `reczxTIpVI8ZJLex0` (Enrollment `rec93mAfo5jKqP3g5`, Week `recT3EXo4Tz7BKFIb`)

## What changed

| Issue | Fix |
|-------|-----|
| General shooting days missing (email showed 4) | `days.size` bug — `shootingDaysLogged` now writes `7` and `7/7` display |
| Goal completion 3,605% | Parent display uses Weekly Threshold tiers — shows **150%+** when ratio ≥ 1.5 |
| Empty video list | Week match uses Submission fallback; payload includes date, name, secure URL |

## Production paste (Mike)

1. Paste **072 v4.7** from GitHub (`072-email-notifications-and-external-handoffs-build-weekly-summary-email-package.js`) — skip GitHub header; paste docblock through end.
2. Paste **074 v3.3** only if Production Automations table still shows v3.2 or older (074 v3.2 already live may work; v3.3 adds `goalCompletionDisplay` forwarding).
3. Do **not** check `Send to Make?` until rebuild verification passes.

## Rebuild verification (Schmidt WAS)

On `reczxTIpVI8ZJLex0`:

1. Clear `Weekly Email Ready?` if needed; check `Build Weekly Email Now?` once.
2. Confirm `Weekly Email Payload JSON` contains:

| Field | Expected |
|-------|----------|
| `canonicalShootingDaysLogged` | `7` |
| `shootingDaysLogged` | `7` |
| `shootingDaysDisplay` | `"7/7"` |
| `canonicalDaysLogged` / `perfectWeekDaysLogged` | `4` |
| `daysLoggedDisplay` | `"4/7"` |
| `canonicalShots` | `48066` |
| `canonicalWeekXp` | `1280` |
| `goalCompletionDisplay` | `"150%+"` (not `3605%`) |
| `videoSubmissionCount` | `8` |
| `videoSubmissions[].secureUrl` | https token URLs where uploaded |
| `perfectWeekCriteria.eligible` | `false` |

3. Confirm HTML shows **Shooting Days Logged: 7/7**, **Perfect Week Qualifying Days: 4/7**, **Goal Completion: 150%+**, and eight video lines.

## Do not send yet

~~Only after payload verification passes should Mike proceed to a controlled send (074 → 079 → Hub).~~

**Update 2026-08-24:** E2E disposable send **complete** (`live-tested`). See [`2026-08-24-weekly-email-e2e-closeout.md`](./2026-08-24-weekly-email-e2e-closeout.md).
