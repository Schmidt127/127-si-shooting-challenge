# Deploy — Automation 057 v1.9 (goal settlement fix)

| Field | Value |
|-------|--------|
| Automation | **057 - Achievements and Milestones - Calculate Perfect Week Eligibility** |
| Version | **1.9** |
| Script | `airtable/automations/shooting-challenge/057-achievements-and-milestones-calculate-perfect-week-eligibility.js` |
| Production base | `appn84sqPw03zEbTT` |
| Automation ID | `wflVRPhgunsosFjWS` |
| Safe test WAS | `reczxTIpVI8ZJLex0` |
| Keep OFF during paste? | **No** — leave Live; paste into the existing Run a script action, then re-test |
| Do not run | **058**, **059** |

## Root cause (verified live)

1. **Script bug (primary):** v1.7/v1.8 compared `Weekly Goal Shots Target` (formula `Goal Shots Target / 9`, e.g. `1333.33…`) to Goal Record `Total Shot Target` (`12000`). Settled goals always failed with *“Weekly goal formula/lookup has not settled…”*.
2. **Builder defect (secondary):** Run a script input `recordId` is **hardcoded** to `reczxTIpVI8ZJLex0` instead of the triggering Weekly Athlete Summary record ID.
3. **Queue side effect:** `Perfect Week Calculation Queue?` is `1` only when Automation Status is **Pending** or **Ready**. After **Error**, Queue = `0`, so the natural trigger will not re-fire until a manual script test or Status returns to Pending/Ready.

## Paste steps (Mike)

1. Open Production automation **057**.
2. Open the **Run a script** action.
3. Select all script text → replace with repository script **from the production docblock** (`/***************************************************************************************************` starting at Version 1.9) **through the final `}`** — skip the GitHub-only header block above that docblock.
4. Confirm header shows **Version: 1.9**.
5. Fix input mapping (required):
   - Input variable name: `recordId`
   - Source: **Airtable record ID** of the **triggering** Weekly Athlete Summary record (dynamic).
   - **Do not** leave a static value `reczxTIpVI8ZJLex0` as the production mapping.
6. Save / update the automation (keep **Live**).

## Safe test (after paste + dynamic mapping)

1. Do **not** edit Goal, Queue, dates, eligibility, or invent submissions.
2. On automation **057**, use **Test** → Run a script with `recordId = reczxTIpVI8ZJLex0` (test override is OK for this one-shot).
3. Expect run history / console JSON similar to:
   ```json
   {"automation":"057","version":"1.9","recordId":"reczxTIpVI8ZJLex0","action":"ready",...}
   ```
4. On WAS `reczxTIpVI8ZJLex0`, expect:
   - `Perfect Week Automation Status` = **Ready**
   - `Perfect Week Automation Error` = blank
   - `Perfect Week Daily Check Status` = **Fail** until seven distinct Sunday–Saturday Mountain days exist (currently six: Aug 16–21)
   - `Perfect Week Eligible?` remains **0** until daily + video + zoom + homework gates all pass (Saturday window)
5. Re-run the same Test once more — helpers may refresh; **no** unlock/XP (058/059 untouched).

## Expected until Saturday

- v1.9 clears the false unsettled-goal Error.
- Daily requirement still needs **Saturday Aug 22** activity for seven distinct days.
- Do not run 058/059 until Eligible = 1 after a natural Ready pass.

## Rollback

Re-paste prior Production body (v1.8) from Airtable revision history if available; otherwise restore from git parent of this commit and re-paste.
