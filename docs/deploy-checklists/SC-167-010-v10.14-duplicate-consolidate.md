# Deploy — Automation 010 v10.14 (SC-167 duplicate SUBMISSION_XP consolidate)

**Backlog:** SC-167  
**Script:** `airtable/automations/shooting-challenge/010-submission-intake-create-xp-event.js`  
**Version:** v10.14  
**Base:** Production `appn84sqPw03zEbTT` (paste after DEV smoke if you keep a DEV copy)

## What changes

- Deterministic consolidate when multiple `SUBMISSION_XP|{submissionId}` rows share exact ownership: keep earliest Created (then lowest id) Active?; deactivate extras.
- Ambiguous ownership still fails closed (no writes).
- Post-create uniqueness recheck heals concurrent-create races.
- Does **not** change XP amounts or Submission eligibility rules.

## Paste steps

**Status (2026-09-05):** Live Automation **010** (`wflJUkUJYTtRWJCyH`) is **v10.14 deployed** — paste complete. **Option A live proof COMPLETE / Live Tested** — [`../audits/SC-167-010-V1014-OPTION-A-LIVE-PROOF-20260905.md`](../audits/SC-167-010-V1014-OPTION-A-LIVE-PROOF-20260905.md). No further paste or formula gate required for SC-167.

1. Open Automation **010 - Submission Intake and Asset Creation - Create XP Event from Submission**.
2. Replace script body with GitHub file contents from the production docblock (`/************************************************************`) through EOF. **Skip** the top GitHub-only header if present.
3. Confirm SCRIPT metadata shows `version: "v10.14"`.
4. Keep existing trigger (`Reconciliation Needed? = 1` + recommended conditions) and `recordId` input.
5. Turn ON (if it was ON).

## Verification (disposable)

**Calendar note:** Production Weeks for this Program Instance are **May–Jul 2027**. With wall-clock `Activity Date Is Future?` / `NOW()`, an Activity Date cannot be both ≤ today and inside a 2027 week. Eligible 010 XP create therefore needs Mike-authorized temporary Season Sim clock formulas (then restore), or the next authorized Season Sim execute.

1. Authorize temporary Season Sim gate for `Activity Date Is Future?` (or bounded Season Sim), **or** wait for next authorized Season Sim.
2. On Schmidt allowlisted enrollment only (`Athlete Email` / Hub allowlist `schmidt@fairfieldbasketballclub.com`), create **one** counted disposable Submission with Activity Date **inside** the linked Week under the sim clock.
3. Wait for 010 success → exactly one Active? XP Event with Source Key `SUBMISSION_XP|{submissionId}`.
4. Clear `Last Reconciled Signature` (sets Needed?=1) and let 010 re-run → **same** XP Event id; still one Active?; `actionOut` repaired/reactivated (not a second create).
5. Optional race stress: only if Mike authorizes overlapping manual runs; expect `consolidated_duplicate_canonical` and one Active?.
6. Delete disposable Submission + its XP Event(s) when done (enrollment-scoped test rows only).
7. **Restore** Production formulas to `NOW()` / `TODAY()` if a temporary gate was used.

Attempt without Season Sim formulas (2026-09-05): Athlete1 Submission with Activity Date `2026-05-10` + Week 1 2027 correctly `skipped_ineligible` (latched, 0 XP). See [`audits/SC-167-168-169-LIVE-VERIFICATION-20260905.md`](../audits/SC-167-168-169-LIVE-VERIFICATION-20260905.md).

## Rollback

Re-paste prior GitHub **v10.13** from `master` history if needed.
