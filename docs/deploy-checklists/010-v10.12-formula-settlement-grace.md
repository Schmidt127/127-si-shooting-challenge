# Deploy — Automation 010 v10.12 (formula/link settlement grace)

| Field | Value |
|-------|--------|
| Automation | **010 - Submission Intake and Asset Creation - Create XP Event from Submission** |
| Version | **v10.12** |
| Script | `airtable/automations/shooting-challenge/010-submission-intake-create-xp-event.js` |
| **Paste file (copy this)** | [`010-v10.12-PASTE.txt`](./010-v10.12-PASTE.txt) |
| Operator packet (2026-08-25) | [`docs/prod-completion/2026-08-25/010-V10.12-PASTE.md`](../prod-completion/2026-08-25/010-V10.12-PASTE.md) |
| Master bundle doc | [`2026-08-23-production-qa-paste-bundle.md`](./2026-08-23-production-qa-paste-bundle.md) |
| Production base | `appn84sqPw03zEbTT` |
| Safe replay test (optional) | `recvtQh5Rq6yTFotc` — only after formulas settle; do not edit the record for this test |
| Keep OFF during paste? | **No** — paste into the existing Run a script action while Live |

## Problem fixed

v10.11 threw error emails when `Reconciliation Needed? = 1` fired before Enrollment, Week, WAS, `Count This Submission?`, or `Total Shots Counted` finished settling. v10.12 returns `skipped_not_ready` instead (no latch ack, no XP writes, no error email).

## Paste steps (Mike)

1. Open Production automation **010**.
2. Open the **Run a script** action.
3. Replace the script body with [`010-v10.12-PASTE.txt`](./010-v10.12-PASTE.txt) (or the repository file from the production docblock through the final `}` — skip the GitHub-only header above that block).
4. Confirm header shows **Version: v10.12** and `SCRIPT.version = "v10.12"`.
5. Confirm input mapping:
   - Variable name: `recordId`
   - Source: **Airtable record ID** of the **triggering Submission** (dynamic — never hardcode a record ID).
6. Update the trigger (see below).
7. Save / keep **Live**.

## Airtable trigger (required)

**Trigger type:** When record matches conditions

**Table:** Submissions

**Conditions (all AND):**

| Field | Operator | Value |
|-------|----------|-------|
| Reconciliation Needed? | = | 1 |
| Enrollment | is not empty | — |
| Week | is not empty | — |
| Weekly Athlete Summary | is not empty | — |
| Count This Submission? | = | 1 |
| Total Shots Counted | > | 0 |

**Input variable:** `recordId` = triggering record's Record ID (dynamic).

**Important:** Airtable only fires this when a record **starts** matching. Turning the automation on while a record already matches does **not** rerun that record.

## Expected run-history outputs

### Temporary formula delay (v10.12 new path)

```json
{
  "statusOut": "skipped",
  "actionOut": "skipped_not_ready",
  "errorOut": "",
  "reconciliationAcknowledged": false,
  "notReadyFields": ["Enrollment", "Count This Submission?"]
}
```

### Valid create

```json
{
  "statusOut": "success",
  "actionOut": "created",
  "errorOut": "",
  "reconciliationAcknowledged": true
}
```

### Replay

```json
{
  "statusOut": "success",
  "actionOut": "repaired_same_event",
  "errorOut": "",
  "reconciliationAcknowledged": true
}
```

(or `reactivated_same_event`)

### Settled ineligible

```json
{
  "statusOut": "skipped",
  "actionOut": "skipped_ineligible",
  "errorOut": "",
  "reconciliationAcknowledged": true
}
```

### Integrity problem

```json
{
  "statusOut": "error",
  "actionOut": "error",
  "errorOut": "FAILED AT: … — …",
  "reconciliationAcknowledged": false
}
```

## Safe test procedure

1. Paste v10.12 and update the trigger.
2. Submit a new test submission (or use a controlled replay record after formulas settle).
3. Confirm a transient early run (if any) shows `skipped_not_ready` with **no** error email.
4. After Enrollment / Week / WAS / count / shots settle, confirm one `SUBMISSION_XP|{submissionId}` event.
5. Re-run once — same event reused (`repaired_same_event` or `reactivated_same_event`), no duplicate.

## Offline validation

```bash
node --test tools/testing/tests/test_010_offline.mjs
node --test tools/testing/tests/test_010_date_key.mjs
node --test tests/pipeline/010-submission-base-multi-family.test.mjs
node --test tests/pipeline/counted-submission-xp-reversal-lifecycle.test.mjs
```

## Rollback

Re-paste v10.11 from Airtable revision history or git parent commit if needed. Restore the prior trigger (Reconciliation Needed? = 1 only) if the stricter gate causes missed runs.
