# 121 v1.0 — Recorded Zoom half-XP operator packet (SC-147)

**Status:** **READY FOR DEV INSTALL** — **NOT Live in Production**  
**Date:** 2026-09-02  
**Backlog:** SC-147 / MRW-H10  
**Automation slot:** **121** (assigned 2026-09-02)  
**GitHub script:** `airtable/automations/shooting-challenge/121-zoom-recording-credit-award-half-xp.js`  
**Design brief:** [`docs/challenge-year/RECORDED-ZOOM-HALF-XP-DESIGN-BRIEF.md`](../challenge-year/RECORDED-ZOOM-HALF-XP-DESIGN-BRIEF.md)

> **Do not paste to Production** until DEV disposable proof passes and Mike approves. Automation **117** remains email-only.

---

## Product decisions (confirmed 2026-08-27, slot assigned 2026-09-02)

| Decision | Value |
|----------|--------|
| Perfect Week | Recording-only credit **does not** count |
| Level gates | Recording credit **does** count |
| XP amount | **Half** live attendance — `floor(ZOOM_ATTEND_BASE / 2)` = **30** when live base = **60** |
| Explicit rule row | Prefer **XP Reward Rules** `Rule Key = ZOOM_RECORDING` when Mike adds row (SC-022) |
| Live vs recording | **Mutually exclusive** per meeting+enrollment — live **101** blocks **121** |
| Automation slot | **121** — distinct from **101** (live XP) and **117** (email) |
| Source Key | `ZOOM_RECORDING_CREDIT\|{enrollmentId}\|{zoomMeetingId}` |

---

## Pre-install checklist (Mike / OMNI)

1. Add **XP Reward Rules** row (if not present):
   - `Rule Key` = `ZOOM_RECORDING`
   - `XP Amount` = **30** (half of live base 60) — or leave absent to use floor(live/2) fallback
   - `Active?` = checked
2. Confirm **Zoom Attendance** fields exist: `Recording Quiz Satisfactory?`, `Zoom Credit Conflict?`, `Enrollment`, `Zoom Meeting`
3. Confirm automation **101** and **117** remain **unchanged**

---

## Airtable automation install (DEV / disposable test first)

| Step | Action |
|------|--------|
| 1 | Create automation **121 - Zoom Recording Credit - Award Half XP** in folder **17 - Zoom Recording Credit** |
| 2 | Trigger: **Zoom Attendance** when record matches conditions |
| 3 | Conditions: `Recording Quiz Satisfactory?` checked; `Zoom Credit Conflict?` ≠ 1; attendance method = recording path |
| 4 | Action: Run script — paste from GitHub docblock through end (skip GitHub header) |
| 5 | Input: `recordId` = triggering Zoom Attendance record ID |
| 6 | Start **OFF**; enable only for disposable Schmidt/VERIFY enrollment test |

---

## Disposable proof (after install)

```bash
node airtable/automations/shooting-challenge/lib/sc-147-zoom-recording-credit.test.js
node tools/testing/post-fut030-verify-matrix.mjs   # MRW-F05/F06/F07 prep on empty base
```

Manual path:

1. Create disposable enrollment (or use VERIFY bootstrap manifest).
2. Create Zoom Meeting + Zoom Attendance on **recording** path with quiz satisfactory.
3. Confirm **no** live **101** XP for same meeting+enrollment.
4. Run/trigger **121** — expect one XP Event, Source Key `ZOOM_RECORDING_CREDIT|…`, **30** XP (when live base = 60).
5. Re-run **121** — expect `skipped_already_awarded`.
6. Confirm Perfect Week formulas unchanged for recording-only week.

---

## Production promotion (Mike approval required)

1. DEV proof evidence saved under `docs/testing/evidence/sc-147/`
2. Mike approves Production paste window
3. Paste **121 v1.0** → test on Schmidt disposable → enable trigger
4. Update `CHANGELOG.md` § Airtable and `CURRENT-TRUTH.md` §8
5. Re-prove **SC-087** live-vs-recording exclusivity

---

## Related (do not confuse)

| Automation | Role |
|------------|------|
| **101** | Live Zoom attendance XP |
| **117 v2.1** | Recording approval **email** only |
| **121 v1.0** | Recording half-XP writer (this packet) |
