# Deploy checklist — Automation 022 v2.0 (GitHub ↔ Production alignment)

**Date:** 2026-08-16  
**Script:** `airtable/automations/shooting-challenge/022-submission-intake-sync-child-upload-writeback-from-submission-asset.js`  
**Production status:** Already **v2.0** and live-tested (Perfect Week video writeback).

## What v2.0 does

- Syncs Submission Asset upload state to the linked Video Feedback (or Homework Completion)
- Uses the **existing** Video Feedback **Upload Status** field (do not create a duplicate)
- Sets **Video URL or Drive Link** from **Reviewer File URL**, then **Canonical File URL**
- Writes **Video Asset File Name** and **Video Asset Uploaded At**
- Confirms **Writeback Complete?** when Upload Status is Uploaded
- Idempotent: re-run does not create child records or duplicate writeback when already synced

## Mike Airtable action

**Default: none.** Production already runs v2.0 successfully.

Optional (only if you want the Airtable script body to match this GitHub file byte-for-byte):

1. Open automation **022** in PROD.
2. Confirm the version comment/string shows **v2.0**.
3. If the editor still shows Drive-first URL selection without Reviewer/Canonical preference, paste from GitHub (docblock through EOF; skip GitHub-only header if present).
4. Re-run once on an already-synced video asset and confirm `skipped_already_synced` or no field churn.

## Offline verification

```bash
node airtable/automations/shooting-challenge/lib/022-child-upload-writeback.test.js
```
