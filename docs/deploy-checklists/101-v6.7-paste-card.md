# Paste card — Automation 101 v6.7 (Zoom attendance reconciliation)

> **Final status (2026-08-21):** Production live script is **v6.7**. This card remains for rollback / re-paste reference only — not an open paste-pending item.

**Date:** 2026-08-21  
**Base:** Production `appn84sqPw03zEbTT`  
**Agents:** Do not change Airtable directly

## Exact Production automation name

`101 - Zoom Attendance XP - Award Meeting XP`

## Trigger and conditions

| Setting | Value |
|---------|--------|
| Trigger | When record matches conditions |
| Table | Zoom Meetings |
| Condition | **Zoom XP Reconciliation Needed?** equals **1** |
| Input | `recordId` = triggering Zoom Meetings record ID |

## Paste artifact

[`101-v6.7-prod-paste.txt`](./101-v6.7-prod-paste.txt) — skip GitHub header; paste from production docblock through end.

Confirm `version: "v6.7"` and `reconciled_awaiting_completed_status` in the pasted body.

## ON / OFF

Paste while **OFF**, then turn **ON** after a controlled Schmidt test (101 is the live Zoom XP writer).

## Controlled recovery for `recxtpMu4ONbdDD45`

1. Paste **101 v6.7** and save.
2. Open Zoom Meeting `recxtpMu4ONbdDD45`.
3. Set **Meeting Status** = **Completed** (required for awards).
4. Wait until **Zoom XP Reconciliation Needed?** = **1** (STATUS change wakes the formula).
5. Confirm 101 runs (or Test with `recordId=recxtpMu4ONbdDD45`).
6. Expect one active `ZOOM_ATTEND_BASE|recxtpMu4ONbdDD45|{enrollmentId}` XP Event per active attendee (5 total), Curtis’s existing event **reactivated** (same Source Key / same event ID if ownership matches), no duplicates.
7. Confirm Needed settles to **0**.

## Rollback

Turn 101 OFF. Re-paste prior v6.6 if needed. Do not delete XP Events as rollback.

## Formula note

No formula paste required. Production `Zoom XP Current Signature` already includes `ATTENDEES=` + `ARRAYJOIN({Attendees})`. The bug was lifecycle **withdrawal** treatment of Scheduled, not a missing Attendees term.
