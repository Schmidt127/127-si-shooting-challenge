# 058 v1.5 — Perfect Week Unlocks field alignment (Production paste)

**Date:** 2026-08-29  
**GitHub:** `airtable/automations/shooting-challenge/058-achievements-and-milestones-create-perfect-week-unlock.js`  
**Also paste (recommended with 058):** `059-…create-xp-event-from-achievement-unlock.js` **v3.7** (Perfect Week reads `Milestone Source Key`; unlock notes = `Coach Note`)

## Why

Production Unlocks has **`Milestone Source Key`** and **`Coach Note`**. Older 058 expected **`Source Key`** / **`Notes`** (XP Events naming) → create failed; SC-PW-E2E timed out on WAS `recl3DmBh22ADPWWe`.

## Paste steps

1. Open Automation **058** in Production.
2. Replace script body with GitHub file **from the production docblock through end** (skip the GitHub-only header comment block at the very top if present).
3. Confirm version comment shows **1.5**.
4. Keep automation **Live**.
5. Confirm trigger: Weekly Athlete Summary **created/updated** (lifecycle — not positive-only / empty-unlock-only).
6. Confirm `recordId` input is dynamically mapped to the triggering WAS record.
7. Paste **059 v3.7** the same way (or confirm live 059 already prefers Milestone Source Key for Perfect Week).

## Controlled proof (existing WAS — do not create a new test week)

1. Manually run **058** on WAS **`recl3DmBh22ADPWWe`**.
2. Unlock **Milestone Source Key** = `PERFECT_WEEK|rec93mAfo5jKqP3g5|recNzl4dNOtDmJqnV`.
3. **059** creates XP Event with **100** XP; unlock XP Award Status = **Awarded**.
4. Re-run **058** → no duplicate unlock.
5. Only then: one qualifying E2E `--apply` + cleanup.

## Do not

- Mark SC-PW-E2E / Perfect Week complete without evidence JSON proving 058+059+100 XP+dedupe.
- Run qualifying `--apply` before steps 1–4 above.
- Create another test week.
- Add a new Unlocks field named `Source Key`.
