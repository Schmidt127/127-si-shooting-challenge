# Deploy — Automation 057 v2.2 (SC-034 Perfect Week video minimum + date-key hardening)

| Item | Value |
|---|---|
| Automation | **057 - Achievements and Milestones - Calculate Perfect Week Eligibility** |
| Repo version | **2.2** (2026-08-27) |
| Script | `airtable/automations/shooting-challenge/057-achievements-and-milestones-calculate-perfect-week-eligibility.js` |
| Rollback tag | `rollback/agent-config-automation-reliability-20260827` |
| **Production status** | **Repaste required** — Automations Code still has typo `MInimum` (MCP 2026-08-30) |
| **Operator packet** | [`057-v2.2-perfect-week-video-minimum-paste.md`](./057-v2.2-perfect-week-video-minimum-paste.md) |
| **Paste file** | [`057-v2.2-PASTE.txt`](./057-v2.2-PASTE.txt) |

## What changed

1. **Date keys** — `addDaysToDateKey` uses explicit UTC calendar formatting (no `toISOString().slice(0, 10)`).
2. **Config-only video minimum** — Requires Config field **`Perfect Week Video Minimum`**. Year-aware Config selection; **fail-closed** on missing table/field or blank/invalid/ambiguous value. **No** `legacyRequiredVideoCount: 3`.

## Verified schema (Production `appn84sqPw03zEbTT`)

| Item | Value |
|------|-------|
| Config field | **`Perfect Week Video Minimum`** (renamed from typo `MInimum` 2026-08-27) |
| Field id | `fldqRxjWGXcbUZUg3` |
| Type | number (precision 0) |
| Values | **3** on each active school-year Config row |

## Production completed (Mike 2026-08-27)

1. Config field renamed to **`Perfect Week Video Minimum`** (typo corrected).
2. WAS lookup **`Config: Perfect Week Video Minimum`** added (Enrollment → `Config - Lnk`).
3. WAS formula **`Perfect Week Video Requirement Met?`** updated:

```
IF({Perfect Week Video Count} >= {Config: Perfect Week Video Minimum}, 1, 0)
```

Repository contract: `lib/config-selection/perfect-week-video-minimum.js`.

## Mike — repaste required (field rename follow-up)

Production **057** still references the old Config field name **`Perfect Week Video MInimum`** in script CONFIG. After the Config field rename, repaste **057 v2.2** from repo so `perfectWeekVideoMinimum` matches live schema.

1. Open Production automation **057**.
2. Replace script body with repo file (skip GitHub header if present).
3. Confirm input variable **`recordId`** (Weekly Athlete Summary).
4. Test on a disposable WAS with known week + submissions.
5. Verify console JSON includes `requiredVideoCount`, `videoMinimumSource: "config_perfect_week_video_minimum"`, and `version: "2.2"`.

## Validation

```bash
node tests/automation-contracts/057-perfect-week-video-minimum.test.js
node tests/automation-contracts/hardcode-forbidden-patterns.test.js
node --test tools/testing/tests/test_057_runtime.mjs
node airtable/automations/shooting-challenge/lib/xp-date-normalization.test.js
```
