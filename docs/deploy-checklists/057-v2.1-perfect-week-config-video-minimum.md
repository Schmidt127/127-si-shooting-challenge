# Deploy — Automation 057 v2.1 (SC-034 Perfect Week video minimum + date-key hardening)

| Item | Value |
|---|---|
| Automation | **057 - Achievements and Milestones - Calculate Perfect Week Eligibility** |
| Repo version | **2.1** (2026-08-27) |
| Script | `airtable/automations/shooting-challenge/057-achievements-and-milestones-calculate-perfect-week-eligibility.js` |
| Rollback tag | `rollback/agent-config-automation-reliability-20260827` |

## What changed

1. **Date keys** — `addDaysToDateKey` uses explicit UTC calendar formatting (no `toISOString().slice(0, 10)`).
2. **Config-driven video minimum** — When Config field **`Perfect Week Video Minimum`** exists, 057 resolves year-aware Config and **fail-closed** on missing/invalid values. Until the field exists, **`legacyRequiredVideoCount: 3`** preserves current WAS alignment.

## Mike — schema prerequisite (before Config path is live)

1. Add numeric Config field **`Perfect Week Video Minimum`** on the Config table (value **3** on each active school-year row).
2. Update WAS formula **`Perfect Week Video Requirement Met?`** from hardcoded `>= 3` to the Config-derived threshold (lookup or formula referencing the same value). Repository contract mirror: `lib/config-selection/perfect-week-video-minimum.js`.

**Current WAS formula (prod snapshot):**

```
IF({Perfect Week Video Count} >= 3, 1, 0)
```

**Target once Config lookup exists on WAS (example — adjust field name to your lookup):**

```
IF({Perfect Week Video Count} >= {Perfect Week Video Minimum (from Config)}, 1, 0)
```

## Paste steps

1. Open Production automation **057**.
2. Replace script body with repo file (skip GitHub header if present).
3. Confirm input variable **`recordId`** (Weekly Athlete Summary).
4. Test on a disposable WAS with known week + submissions.
5. Verify console JSON includes `requiredVideoCount`, `videoMinimumSource`, and `version: "2.1"`.

## Validation

```bash
node tests/automation-contracts/057-perfect-week-video-minimum.test.js
node --test tools/testing/tests/test_057_runtime.mjs
node airtable/automations/shooting-challenge/lib/xp-date-normalization.test.js
```
