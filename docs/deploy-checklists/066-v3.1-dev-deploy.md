# Deploy checklist — Automation 066 v3.1 (DEV first)

**Backlog:** H-002 · V2-015 (blocks V2-015 `done`)  
**Status:** **Pending** — waiting on OMNI to confirm Schmidt submission completed intake pipeline + expected milestone behavior  
**Base:** Development only — `appTetnuCZlCZdTCT`  
**Production:** `appn84sqPw03zEbTT` — **do not paste until DEV pass**

**Script (GitHub):** [066-achievements-and-milestones-create-shot-milestone-unlocks.js](../../airtable/automations/shooting-challenge/066-achievements-and-milestones-create-shot-milestone-unlocks.js)  
**Standard:** [v2/06-automation-standards.md](../v2/06-automation-standards.md)  
**Promotion rule:** This checklist is the official promotion document for 066 — [v2/04 § Official promotion documentation](../v2/04-ai-development-standards.md#official-promotion-documentation-required)

---

## OMNI pre-check (2026-07-05) — confirmed

| Item | Status |
|------|--------|
| Automation **066** exists in DEV | ✓ |
| Automation **ON** in DEV | ✓ |
| Trigger table | **Enrollments** ✓ |
| Trigger condition | **Run Shot Milestone Check?** checked ✓ |

## Discovery (2026-07-05)

After the DEV base was cloned, **intake automations were OFF**. Mike turned them **ON**.

This may explain why the manually created **Schmidt** submission did not move through the pipeline — the row may have been created while automations were still disabled, not because the row shape was wrong.

**Do not change Production.**

**066 DEV sandbox test remains pending** until OMNI confirms:

| Item | Owner | Status |
|------|-------|--------|
| Schmidt (test) **Submission** has moved through the normal intake pipeline (**023**, **005**, **009**, **010**, **031**, …) with automations **ON** | OMNI | **Pending** |
| **Expected milestone behavior** for that enrollment (thresholds, expected `actionOut`, unlock creates vs skip) | OMNI | **Pending** |

Until OMNI confirms both: **no `Run Shot Milestone Check?` trigger** for the 066 sandbox test.

---

## Sandbox test — pending (do not run yet)

**Do not** run the **066** sandbox test until OMNI confirms pipeline completion + expected milestone behavior (see **Discovery** above).

**C-020 note:** Manual DEV Submissions remain unreliable if intake automations are OFF or the row is not Fillout-shaped. With automations ON, an existing Schmidt submission may still be valid once OMNI verifies intake completed. See [C-020 justification](../testing-and-intake-architecture.md#c-020-justification--manual-submissions-are-not-enough).

### If Schmidt submission did not complete intake

| Path | Requirement |
|------|-------------|
| **A — Re-run / new submission** | Create or update Schmidt submission **with automations ON**; verify intake chain in Testing views |
| **B — C-020** | **Testing Scenarios** script creates a **Fillout-shaped**, pipeline-ready Submission |
| **C — Existing DEV row** | Identify another **real DEV Submission** that already completed intake for a test enrollment |

Until pipeline-ready state is confirmed: **no Production changes**.

---

## Paste instructions

1. Open **DEV** base → Automations → **066 - Achievements and Milestones - Create Shot Milestone Unlocks**.
2. Open the **Run script** action.
3. From GitHub, copy **line 26 through end** (starts at `/*********` docblock — **skip** lines 1–24 GitHub-only header).
4. Replace entire script body in Airtable.
5. Save automation (leave **ON** in DEV).

### Automation input variable

| Input | Value |
|-------|--------|
| `recordId` | Airtable record ID from trigger → **Enrollment record ID** |

### Script action outputs (map in Airtable if not already)

| Output | Type |
|--------|------|
| `statusOut` | Text |
| `actionOut` | Text |
| `errorOut` | Text |
| `debugStep` | Text |
| `enrollmentIdOut` | Text |
| `createdUnlocksOut` | Number |
| `updatedUnlocksOut` | Number |
| `skippedExistingUnlocksOut` | Number |

### Trigger (confirm matches docblock)

| Item | Expected |
|------|----------|
| Table | **Enrollments** |
| Condition | **Run Shot Milestone Check?** is checked |

---

## Pre-paste verification (DEV)

```
[x] Automation 066 exists in DEV (OMNI confirmed 2026-07-05)
[x] Automation ON; trigger Enrollments + Run Shot Milestone Check? (OMNI confirmed)
[x] DEV intake automations turned ON after clone (Mike 2026-07-05) — verify before new test submissions
[ ] DEV base ID is appTetnuCZlCZdTCT
[ ] Email/upload automations OFF or on dev Make (see development-base-setup.md Step 3)
[ ] OMNI confirms Schmidt submission completed intake pipeline (automations ON)
[ ] OMNI provides expected milestone behavior for test enrollment
[ ] Do **not** trigger 066 sandbox until both OMNI confirmations received
```

---

## Test plan (one enrollment)

**Pending** — OMNI must confirm Schmidt submission completed intake + expected milestone behavior. See **Discovery** above.

Do **not** trigger **Run Shot Milestone Check?** until OMNI delivers both confirmations.

| Field | Fill when OMNI delivers |
|-------|-------------------------|
| Enrollment `rec…` | |
| Grade Band | |
| Total counted shots (baseline) | |
| Milestone(s) expected to unlock | |
| Expected `actionOut` | `created` / `updated` / `skipped_existing` / other |
| Expected unlock count delta | |

### Steps

1. Note enrollment `rec…` and current unlock count for that enrollment.
2. Check **Run Shot Milestone Check?** on the enrollment (or use existing trigger path).
3. Wait for automation run → open run history.
4. **Pass if:**
   - `statusOut` = `success` (or `skipped` with expected `actionOut` e.g. `skipped_existing`)
   - `errorOut` empty
   - `debugStep` reaches final step
   - Console JSON shows `"version": "v3.1"`
   - New **Athlete Achievement Unlock** rows have **Milestone Source Key** `SHOT_MILESTONE|{enrollmentId}|{shotMilestoneId}`
   - **Week** populated on new unlocks when **Milestone Activity Date** maps to a **Weeks** row
   - **Run Shot Milestone Check?** cleared on success/skip (not left checked on error)
5. Re-check same enrollment (idempotent): second run should **not** duplicate unlocks (`skipped_existing` or zero creates).

### Idempotency re-run

```
[ ] Second trigger on same enrollment → no duplicate unlock rows (same Milestone Source Key)
```

---

## Audit (DEV)

Run in **DEV** base Scripting extension (read-only):

| Order | Script |
|-------|--------|
| 1 | [audit-final-090f-athlete-achievement-unlocks-workflow.js](../../airtable/extension-scripts/audits/audit-final-090f-athlete-achievement-unlocks-workflow.js) |

**Note:** 090F scopes to **Active?** enrollments. For inactive test enrollments, review unlock rows manually or temporarily test on an active test enrollment.

**Pass criteria:** No unexpected duplicate groups; shot milestones dedupe on **Milestone Source Key** (audit v1.1 logic).

Optional pipeline audit:

| Script |
|--------|
| [audit-achievement-xp-pipeline-integrity.js](../../airtable/extension-scripts/audits/audit-achievement-xp-pipeline-integrity.js) (Stage I) |

---

## After DEV pass

| Step | Owner |
|------|-------|
| 1. Mark H-002 DEV test complete in this doc (date + enrollment tested) | Mike |
| 2. Mark V2-015 criterion 4 **Done** in [v2-015-development-base-architecture.md](./v2-015-development-base-architecture.md) | Cursor |
| 3. Paste **same script** into **production** 066 | Mike |
| 4. Re-run 090F on prod (dry-run) | Mike |
| 5. Update [CHANGELOG.md](../CHANGELOG.md) under `### Airtable` | Cursor |
| 6. Update [automation-index.md](./automation-index.md) — 066 pasted DEV + PROD | Cursor |

---

## Test log (fill in after DEV run)

| Field | Value |
|-------|--------|
| Date | |
| Base | DEV `appTetnuCZlCZdTCT` |
| Enrollment tested | `rec…` |
| `statusOut` | |
| `actionOut` | |
| Unlocks created | |
| Week written | Yes / No |
| Idempotent re-run | Pass / Fail |
| 090F audit | Pass / Fail |
| Tested by | Mike |

---

## Revision log

| Date | Notes |
|------|-------|
| 2026-07-05 | Checklist created — first V2 dev-first automation deploy |
| 2026-07-05 | OMNI confirmed 066 ON in DEV (Enrollments / Run Shot Milestone Check?); sandbox test **waiting** on enrollment `rec…` + expected milestone behavior |
| 2026-07-05 | **Blocked** — manual DEV Submissions unreliable; need pipeline-ready Fillout-shaped row (C-020) or existing DEV submission; do not test 066 with incomplete manual rows |
| 2026-07-05 | **Discovery** — DEV intake automations were OFF after clone; Mike turned ON; may explain Schmidt manual submission not moving; **pending** OMNI confirm intake complete + expected milestone behavior |
