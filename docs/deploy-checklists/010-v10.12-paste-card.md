# Automation 010 v10.12 — paste card (formula/link not-ready fix)

> **Production is not fixed until Mike pastes v10.12, updates the trigger, and verifies run history.**

| Field | Value |
|-------|--------|
| Automation | **010** — Submission Intake and Asset Creation — Create XP Event from Submission |
| GitHub version | **v10.12** (2026-08-22) |
| Production before paste | **v10.11** (confirm in Automations UI) |
| Source file | `airtable/automations/shooting-challenge/010-submission-intake-create-xp-event.js` |
| Problem fixed | False error emails when Enrollment / Week / WAS / counted fields were still settling |

## What changed in v10.12

1. **Trigger** — Added readiness filters so the automation does not fire on an empty latch alone.
2. **Script** — Temporary formula/link lag returns `skipped_not_ready` without throwing; latch stays open for retry.
3. **Permanent ineligible** — Same as v10.11: `skipped_ineligible` or deactivate owned event with latch acknowledgement.

## Airtable builder steps (plain English)

1. Open **Automations** → **010 - Submission Intake and Asset Creation - Create XP Event from Submission**.
2. Turn the automation **OFF** before editing (capture current script + trigger screenshots).
3. Open the **Run script** action → replace the script with GitHub source from the docblock through end (skip the GitHub-only header block at the top of the file).
4. Confirm script metadata reads **v10.12** in the docblock and `SCRIPT.version`.
5. Open the trigger **When record matches conditions** on table **Submissions**.
6. Set **all** of these conditions (AND):
   - `Reconciliation Needed?` equals **1**
   - `Enrollment` is not empty
   - `Week` is not empty
   - `Weekly Athlete Summary` is not empty
   - `Count This Submission?` is checked
   - `Total Shots Counted` is greater than **0**
7. Under the script action, confirm the input variable:
   - **Name:** `recordId`
   - **Value:** dynamic — the triggering Submission record ID (not a hardcoded `rec…` test ID)
8. Save the automation. Turn **ON** only after a controlled manual script run passes on a known eligible Submission.
9. Do **not** modify Production record `recvtQh5Rq6yTFotc` as part of this paste.

## Expected run-history outputs

### Temporary not-ready (formula/link still settling)

| Output | Expected |
|--------|----------|
| `statusOut` | `skipped` |
| `actionOut` | `skipped_not_ready` |
| `errorOut` | empty |
| `reconciliationAcknowledged` | `false` |
| `debugStep` | `5 - Temporary not-ready (formula/link settlement)` |
| Automation run | **Success** (no Airtable error email) |
| `Reconciliation Needed?` | stays **1** until fields settle and a later run succeeds |

### Eligible settled submission (create)

| Output | Expected |
|--------|----------|
| `statusOut` | `success` |
| `actionOut` | `created` |
| `errorOut` | empty |
| `reconciliationAcknowledged` | `true` |
| `sourceKey` | `SUBMISSION_XP\|{Submission ID}` |
| `Reconciliation Needed?` | settles to **0** after run |

### Eligible replay (owned event exists)

| Output | Expected |
|--------|----------|
| `statusOut` | `success` |
| `actionOut` | `reactivated_same_event` or `repaired_same_event` |
| XP Event count | exactly one canonical `SUBMISSION_XP\|{Submission ID}` |

### Permanent ineligible (structurally ready, Count This Submission? false)

| Output | Expected |
|--------|----------|
| `statusOut` | `skipped` or `success` (if owned event deactivated) |
| `actionOut` | `skipped_ineligible` or `deactivated_same_event` |
| `reconciliationAcknowledged` | `true` |
| XP created | none (or owned event deactivated) |

### True integrity error (ambiguous links, ownership mismatch)

| Output | Expected |
|--------|----------|
| `statusOut` | `error` |
| `actionOut` | `error` |
| Automation run | Failed (actionable error email) |

## Offline regression (run before paste)

```bash
node --test tools/testing/tests/test_010_offline.mjs
node --test tools/testing/tests/test_010_not_ready.mjs
node --test tools/testing/tests/test_010_date_key.mjs
node --test tests/pipeline/010-submission-base-multi-family.test.mjs
```

## Post-paste verification (Mike)

1. Manual script run on an eligible Submission with `Reconciliation Needed? = 1` → `success` + latch clears.
2. Replay same Submission → same `xpEventId`, no duplicate.
3. If a not-ready row appears in backlog briefly, confirm run shows `skipped_not_ready` and **no** error email.
4. Capture run ID, outputs JSON, and `Reconciliation Needed?` before/after.

## Rollback

1. Turn **010 OFF**.
2. Restore captured v10.11 script and trigger from pre-paste screenshots.
3. Leave Submission / XP Event records unchanged.
