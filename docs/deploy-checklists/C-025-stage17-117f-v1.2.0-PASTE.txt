C-025 Stage 17 � Automation 117f v1.2.0 PASTE BODY
====================================================
** SUPERSEDED - HISTORICAL PASTE PACKET (v1.2.0). ** The current shipped script is
117f v1.1 (pure Make handoff, writes NO Airtable records; Make owns dedupe). The
v1.2.0 body below (which stamped Airtable fields and used the older THREE-part send
key ZOOM_REC_EMAIL|{Enrollment RID}|{Zoom Meeting RID}) is retained only for
historical evidence. Canonical send key is now the FOUR-part
ZOOM_REC_EMAIL|{Enrollment RID}|{Zoom Meeting RID}|{Zoom Attendance RID}.
Authoritative current state: airtable/automations/shooting-challenge/117f-zoom-recording-send-approval-email.js
and docs/deploy-checklists/C-025-117f-prod-zoom-recording-approval-email.md. Do NOT paste this v1.2.0 body.
----------------------------------------------------
Base: DEV appTetnuCZlCZdTCT ONLY
Automation name: 117f - Zoom Recording Credit - Send Approval Email
Folder: 17 - Zoom Recording Credit
Keep OFF after paste. Inputs: recordId, webhookUrl (leave blank until controlled test).
Do NOT paste webhook secrets into git. Do NOT enable PROD.
Source: airtable/automations/shooting-challenge/117f-zoom-recording-send-approval-email.js
Paste everything below this line into the Airtable script action (exclude this header).
----------------------------------------------------
/************************************************************

 * 117f - Zoom Recording Credit - Send Approval Email
 *
 * Version: v1.2.0
 * Date Written: 2026-07-14
 * Last Updated: 2026-07-20
 *
 * Architecture: Stage 17 Zoom Attendance path (S16 HC path superseded)
 *
 * PURPOSE
 * - Parent approval email via Make webhook after Satisfactory (117f only).
 * - Automation 117 does NOT send email and must keep webhookUrl blank.
 *
 * IMPORTANT DESIGN RULES
 * - Send key: ZOOM_REC_EMAIL|{Enrollment RID}|{Zoom Meeting RID}
 * - Stamp Send Key + Sent At ONLY after HTTP 2xx
 * - Blank webhook / disabled config / conflict / already sent → skip (no stamp)
 * - Non-2xx / fetch failure → error status; no stamp; leave retryable
 *
 * INPUT
 * - recordId (required) — Zoom Attendance record id
 * - webhookUrl (optional) — blank ⇒ skipped_no_webhook (never invent a URL)
 *
 * OUTPUTS
 * - statusOut: success | skipped | error
 * - errorOut
 * - debugStep
 * - actionOut
 * - sendKey
 *
 * TRIGGER
 * - Zoom Attendance — Recording Quiz path after Satisfactory (keep OFF until approved)
 *
 * AUTOMATION NAME
 * - 117f - Zoom Recording Credit - Send Approval Email
 *
 * FOLDER
 * - 17 - Zoom Recording Credit
 ************************************************************/

// @ts-nocheck

const SCRIPT = {
  scriptName: "117f-zoom-recording-send-approval-email",
  version: "v1.2.0",
  versionDate: "2026-07-20",
  originalWrittenDate: "2026-07-14",
  lastUpdated: "2026-07-20",
  folder: "17 - Zoom Recording Credit",
  automationName: "117f - Zoom Recording Credit - Send Approval Email",
};

const CONFIG = {
  tables: { zoomAttendance: "Zoom Attendance" },
  fields: {
    attendanceMethod: "Attendance Method",
    satisfactory: "Recording Quiz Satisfactory?",
    conflict: "Zoom Credit Conflict?",
    emailEnabled: "Effective Recording Approval Email Enabled?",
    emailTiming: "Effective Recording Approval Email Timing",
    emailTemplate: "Effective Recording Approval Email Template Key",
    enrollmentRid: "Enrollment RID",
    zoomMeetingRid: "Zoom Meeting RID",
    sendKey: "Recording Approval Email Send Key",
    sentAt: "Recording Approval Email Sent At",
  },
  methods: { recordingQuiz: "Recording Quiz" },
  sendKeyPrefix: "ZOOM_REC_EMAIL",
};

function setOutputSafe(key, value) {
  try {
    if (typeof output !== "undefined" && output && typeof output.set === "function") {
      output.set(key, value);
    }
  } catch (e) {
    console.log(`setOutputSafe(${key}) failed: ${e && e.message ? e.message : e}`);
  }
}

function requireRecId(recordId) {
  if (!recordId || typeof recordId !== "string" || !recordId.startsWith("rec")) {
    throw new Error(`Invalid recordId: ${recordId}`);
  }
  return recordId;
}

function getText(record, fieldName) {
  try {
    const v = record.getCellValueAsString(fieldName);
    return v == null ? "" : String(v).trim();
  } catch (e) {
    return "";
  }
}

function getCheckbox(record, fieldName) {
  const v = record.getCellValue(fieldName);
  if (v === true || v === 1 || v === "1") return true;
  if (Array.isArray(v) && v.length === 1) return getCheckbox({ getCellValue: () => v[0] }, "x");
  return false;
}

function normalizeBoolean(value) {
  if (value === true || value === 1 || value === "1") return true;
  if (value === false || value === 0 || value === "0") return false;
  if (value == null || value === "") return null;
  if (Array.isArray(value)) {
    if (value.length === 0) return null;
    if (value.length === 1) return normalizeBoolean(value[0]);
    return null;
  }
  if (typeof value === "object" && value && "name" in value) {
    return normalizeBoolean(value.name);
  }
  const s = String(value).trim().toLowerCase();
  if (s === "yes" || s === "true" || s === "checked") return true;
  if (s === "no" || s === "false" || s === "unchecked") return false;
  return null;
}

function isZoomCreditConflict(value) {
  if (value === true || value === 1 || value === "1") return true;
  if (Array.isArray(value) && value.length === 1) return isZoomCreditConflict(value[0]);
  const n = Number(value);
  if (Number.isFinite(n) && n === 1) return true;
  return false;
}

function fieldExists(table, name) {
  try {
    table.getField(name);
    return true;
  } catch (e) {
    return false;
  }
}

function buildSendKey(enrollmentRid, meetingRid) {
  if (!enrollmentRid || !meetingRid) return null;
  return `${CONFIG.sendKeyPrefix}|${enrollmentRid}|${meetingRid}`;
}

async function updateRecordSafe(table, recordId, fields) {
  const keys = Object.keys(fields || {});
  if (!keys.length) return;
  await table.updateRecordAsync(recordId, fields);
}

function finishSkipped(actionOut, sendKey) {
  setOutputSafe("statusOut", "skipped");
  setOutputSafe("actionOut", actionOut);
  setOutputSafe("errorOut", "");
  if (sendKey) setOutputSafe("sendKey", sendKey);
  setOutputSafe("debugStep", "complete");
}

function finishError(actionOut, errorOut, sendKey) {
  setOutputSafe("statusOut", "error");
  setOutputSafe("actionOut", actionOut);
  setOutputSafe("errorOut", errorOut || "");
  if (sendKey) setOutputSafe("sendKey", sendKey);
  setOutputSafe("debugStep", "error");
}

async function main() {
  let debugStep = "1 - Validate input";
  setOutputSafe("debugStep", debugStep);
  setOutputSafe("errorOut", "");
  setOutputSafe("sendKey", "");

  const cfg = input.config();
  const recordId = requireRecId(String(cfg.recordId || "").trim());
  const webhookUrl = String(cfg.webhookUrl || "").trim();

  const zaTable = base.getTable(CONFIG.tables.zoomAttendance);
  const fieldNames = Object.values(CONFIG.fields).filter((n) => fieldExists(zaTable, n));
  const rec = await zaTable.selectRecordAsync(recordId, { fields: fieldNames });
  if (!rec) throw new Error(`Zoom Attendance not found: ${recordId}`);

  debugStep = "2 - Evaluate send gates";
  setOutputSafe("debugStep", debugStep);

  if (getText(rec, CONFIG.fields.attendanceMethod) !== CONFIG.methods.recordingQuiz) {
    finishSkipped("skipped_not_recording_quiz");
    return;
  }
  if (!getCheckbox(rec, CONFIG.fields.satisfactory)) {
    finishSkipped("skipped_not_satisfactory");
    return;
  }

  const conflictRaw = fieldExists(zaTable, CONFIG.fields.conflict)
    ? rec.getCellValue(CONFIG.fields.conflict)
    : null;
  if (isZoomCreditConflict(conflictRaw)) {
    finishSkipped("skipped_conflict");
    return;
  }

  const enabled = fieldExists(zaTable, CONFIG.fields.emailEnabled)
    ? normalizeBoolean(rec.getCellValue(CONFIG.fields.emailEnabled))
    : null;
  if (enabled !== true) {
    finishSkipped("skipped_disabled");
    return;
  }

  const template = fieldExists(zaTable, CONFIG.fields.emailTemplate)
    ? getText(rec, CONFIG.fields.emailTemplate)
    : "";
  if (!template) {
    finishSkipped("skipped_missing_template_key");
    return;
  }

  const enrollRid = getText(rec, CONFIG.fields.enrollmentRid);
  const meetingRid = getText(rec, CONFIG.fields.zoomMeetingRid);
  const sendKey = buildSendKey(enrollRid, meetingRid);
  if (!sendKey) {
    finishSkipped("skipped_missing_enrollment_or_meeting");
    return;
  }
  setOutputSafe("sendKey", sendKey);

  const sentAtBlank =
    !fieldExists(zaTable, CONFIG.fields.sentAt) || !rec.getCellValue(CONFIG.fields.sentAt);
  if (!sentAtBlank) {
    finishSkipped("skipped_already_sent", sendKey);
    return;
  }

  const existingSendKey = fieldExists(zaTable, CONFIG.fields.sendKey)
    ? getText(rec, CONFIG.fields.sendKey)
    : "";
  if (existingSendKey && existingSendKey === sendKey) {
    finishSkipped("skipped_already_sent", sendKey);
    return;
  }

  if (!webhookUrl) {
    finishSkipped("skipped_no_webhook", sendKey);
    return;
  }

  const timing =
    (fieldExists(zaTable, CONFIG.fields.emailTiming) && getText(rec, CONFIG.fields.emailTiming)) ||
    "On Satisfactory";

  const payload = {
    automationNumber: "117f",
    templateKey: template,
    sendKey,
    enrollmentRid: enrollRid,
    zoomMeetingRid: meetingRid,
    zoomAttendanceId: recordId,
    timing,
  };

  debugStep = "3 - POST to Make webhook";
  setOutputSafe("debugStep", debugStep);

  let resp;
  try {
    resp = await fetch(webhookUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
    });
  } catch (fetchError) {
    const msg = fetchError && fetchError.message ? fetchError.message : String(fetchError);
    finishError("error_webhook_network", msg, sendKey);
    console.log(
      JSON.stringify({
        automation: SCRIPT.scriptName,
        version: SCRIPT.version,
        statusOut: "error",
        actionOut: "error_webhook_network",
        errorOut: msg,
        stamped: false,
      })
    );
    return;
  }

  let bodyText = "";
  try {
    bodyText = await resp.text();
  } catch (e) {
    bodyText = "";
  }

  if (!resp || !resp.ok) {
    const status = resp && resp.status != null ? resp.status : "unknown";
    const msg = `Webhook failed HTTP ${status}: ${String(bodyText).slice(0, 500)}`;
    finishError("error_webhook_http", msg, sendKey);
    console.log(
      JSON.stringify({
        automation: SCRIPT.scriptName,
        version: SCRIPT.version,
        statusOut: "error",
        actionOut: "error_webhook_http",
        errorOut: msg,
        stamped: false,
      })
    );
    return;
  }

  debugStep = "4 - Stamp send key after HTTP 2xx";
  setOutputSafe("debugStep", debugStep);
  const stamp = {};
  if (fieldExists(zaTable, CONFIG.fields.sendKey)) stamp[CONFIG.fields.sendKey] = sendKey;
  if (fieldExists(zaTable, CONFIG.fields.sentAt)) stamp[CONFIG.fields.sentAt] = new Date();
  if (Object.keys(stamp).length) await updateRecordSafe(zaTable, recordId, stamp);

  setOutputSafe("statusOut", "success");
  setOutputSafe("actionOut", "sent");
  setOutputSafe("sendKey", sendKey);
  setOutputSafe("errorOut", "");
  setOutputSafe("debugStep", "complete");
  console.log(
    JSON.stringify({
      automation: SCRIPT.scriptName,
      version: SCRIPT.version,
      actionOut: "sent",
      sendKey,
      stamped: true,
    })
  );
}

try {
  await main();
} catch (err) {
  const msg = err && err.message ? err.message : String(err);
  setOutputSafe("statusOut", "error");
  setOutputSafe("errorOut", msg);
  setOutputSafe("actionOut", "error");
  setOutputSafe("debugStep", "error");
  console.log(
    JSON.stringify({
      automation: SCRIPT.scriptName,
      version: SCRIPT.version,
      statusOut: "error",
      errorOut: msg,
      stamped: false,
    })
  );
  throw err;
}
