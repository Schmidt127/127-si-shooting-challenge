# Operator packet — Automation 071 v4.3 (Homework Feedback Hub handoff)

**Status:** **Paste pending** — GitHub source of truth; Hub **FUT-046** deployed  
**Date:** 2026-09-01  
**Backlog:** FUT-046 (subject contract) · pairs with Hub PR **#43** (merged)  
**Prior checklist:** [`071-v4.1-pha-grade-band-metadata.md`](./071-v4.1-pha-grade-band-metadata.md) (v4.1 PHA Grade Band fix)

| Item | Value |
|------|--------|
| Automation | **071 - Email, Notifications, and External Handoffs - Create Homework Feedback Communications Hub Handoff** |
| Folder | **07 - Email, Notifications, and External Handoffs** |
| Repo / intended | **v4.3** |
| Script | `airtable/automations/shooting-challenge/071-email-notifications-and-external-handoffs-send-homework-feedback-email-webhook.js` |
| **Paste range** | Production docblock (`/************************************************************` or `Version: v4.3` block) **through end of file** — **skip** GitHub-only header lines at top (`GitHub header`, `Automation:`, `System:`, `Source:`, `Status:`) |
| Hub dependency | Communications Hub **FUT-046** subject builder (already live) |
| Mike approval | Required before Production paste |

---

## Diff summary vs prior checklists

| Version | Checklist / note | What changed |
|---------|------------------|--------------|
| **v4.1** | [`071-v4.1-pha-grade-band-metadata.md`](./071-v4.1-pha-grade-band-metadata.md) | PHA **Grade Band** metadata-only — no longer reject multi-band PHA rows |
| **v4.2** | [`homework-feedback-email-redesign-2026-08-22.md`](./homework-feedback-email-redesign-2026-08-22.md) | Branded payload enrichment: `weekName`, `reviewStatus`, canonical URLs, submitted-work links |
| **v4.3** | **This packet** | **FUT-046** — payload adds public **`assignmentTitle`**, **`athleteFirstName`**, **`athleteLastName`** (Hub builds subject; 071 does not set subject string) |

**Unchanged from v4.1:** trigger conditions, Handoff Key, idempotency, PHA match keys (PI + Week + Homework + Slot), Reviewer File URL-only asset path, Hub-only send plane (**079** → Resend). **071 still does not** write `Parent Feedback Sent?` / `Parent Feedback Sent On` (FUT-032 Hub writeback).

---

## Contract snapshot

| Item | Value |
|------|--------|
| Trigger table | **Homework Completions** |
| Trigger type | When record matches conditions |
| Conditions (all AND) | `Parent Feedback Ready?` checked; `Parent Feedback Sent?` unchecked; `Satisfactory?` checked; **Award Status = Awarded**; **Coach Feedback** not empty; **Enrollment** linked |
| Dynamic `recordId` | Triggering **Homework Completion** Record ID |
| Optional input | `testMode` — default **true** for controlled Hub sends |
| Handoff Key | `HOMEWORK_FEEDBACK\|HOMEWORK_COMPLETIONS\|{Homework Completion Record ID}` |
| Event Type / Template Key | `HOMEWORK_FEEDBACK` / `HOMEWORK_FEEDBACK` |
| Downstream | **079** → Communications Hub → Resend |
| Schema changes | **None** |

### v4.3 payload fields (verify in queue JSON)

| Field | Source | Purpose |
|-------|--------|---------|
| `assignmentTitle` | Homework Library public name (Assignment Title → Display → Full Name) | Hub subject segment |
| `athleteFirstName` / `athleteLastName` | Enrollment | Hub subject segment |
| `homeworkTitle` / `homeworkLabel` | Same as `assignmentTitle` | Backward-compatible Hub keys |
| `athleteName` | Enrollment full name | Body copy |
| Prior v4.2 fields | URLs, week, review status, XP, submitted files / quiz | Unchanged |

Hub subject (Hub-owned): `[TEST] Homework Feedback – {First Last} – {Assignment Name}` when `testMode: true`. See [`FUT-046-homework-feedback-subject.md`](./FUT-046-homework-feedback-subject.md).

---

## Pre-paste checklist

- [ ] Confirm Production automation **071** name matches table above (not legacy “Send Homework Feedback Email Webhook”).
- [ ] Confirm **065** is **v10.5+** and **079** Hub dispatcher is Live.
- [ ] Confirm Communications Hub **FUT-046** is deployed (subject builder live).
- [ ] Confirm **FUT-032** Hub writeback fields exist on Homework Completions (Delivery Status, Hub Event ID, etc.).
- [ ] Open GitHub script; confirm docblock **Version: v4.3** and SCRIPT block `version: "v4.3"`.
- [ ] **DEV first** — do not paste Production until DEV smoke passes.
- [ ] Input **`recordId`** must remain **dynamic** (`$ref` → trigger record id) — never hardcode `rec…`.
- [ ] Leave automation **ON** during paste if possible; pause only while editing script body.

---

## Paste steps

1. Open target base automation **071** (DEV, then Production after proof).
2. Open the **Run script** action.
3. Confirm input mapping: **`recordId`** = dynamic Homework Completion ID from trigger.
4. Replace script body: copy from repo file starting at production docblock through EOF; **omit** GitHub header lines 1–6.
5. Save. Confirm SCRIPT metadata shows **v4.3** / **lastUpdated: 2026-09-01**.
6. Map script outputs if not already wired: `statusOut`, `actionOut`, `queueRecordId`, `handoffKey`, `errorOut`, `debugStep`.
7. Update Automations Code tracker (if used) to **v4.3**.

---

## Post-paste smoke — Schmidt HC path (DEV)

Use a **disposable Schmidt** Homework Completion with homework XP already awarded. Known quiz-path fixture (may need gate refresh after FUT-030 reset): HC `recrBnHbLvDpFyIeO` — confirm row still exists in **Testing - Schmidt Homework Completions** view before use.

### Setup gates (Homework Completion)

- [ ] `Parent Feedback Ready?` = checked  
- [ ] `Parent Feedback Sent?` = unchecked  
- [ ] `Satisfactory?` = checked  
- [ ] **Award Status** = **Awarded**  
- [ ] **Coach Feedback** populated  
- [ ] **Enrollment** = Schmidt testing enrollment  
- [ ] Linked PHA present (Grade Band may list multiple bands — must **not** error)

### Run

1. Run **071** with **`testMode: true`** (default).
2. Expect `statusOut=success`, `actionOut=created_handoff` or `existing_handoff`.
3. Confirm **one** Email Handoff Queue row:
   - Handoff Key `HOMEWORK_FEEDBACK|HOMEWORK_COMPLETIONS|{hcId}`
   - Status **Ready**
   - Payload includes **`assignmentTitle`**, **`athleteFirstName`**, **`athleteLastName`**
4. Run **079** → Hub ingest (DEV/staging Hub if available).
5. Confirm Hub Message subject matches FUT-046 contract with **`[TEST]`** prefix.
6. Confirm Resend delivery to allowlisted parent email; **FUT-032** writeback sets `Parent Feedback Sent?` + `Parent Feedback Sent On` on HC.
7. Re-trigger **071** on same HC → `existing_handoff` or skip; **no duplicate email**.

### Stop conditions

- `actionOut=needs_review` with payload conflict — inspect queue row before clearing gates.
- `PHA Grade Band mismatch` — Production still on pre-v4.1 body; re-paste v4.1+ chain.
- Missing `assignmentTitle` — populate Homework Library **Assignment Title** for linked homework.

---

## Offline verification (repo — run before paste)

```bash
node tests/email/automation-071-073-source-safety.test.js
node tests/email/automation-074-117-hub-handoff.test.js
node tests/homework/automation-071-reviewer-file-url.test.js
node tests/homework/pha-grade-band-metadata-contract.test.js
```

---

## Rollback

| Action | When |
|--------|------|
| Turn automation **OFF** | Unexpected queue spam or payload errors |
| Restore prior script from git | `git show master~1:airtable/automations/shooting-challenge/071-email-notifications-and-external-handoffs-send-homework-feedback-email-webhook.js` (or last known good SHA) — re-paste docblock → EOF |
| Do **not** delete Email Handoff Queue rows in bulk | Mark Needs Review individually if needed |
| Hub subject rollback | Separate Communications Hub deploy — 071 rollback alone reverts payload fields only |

**Risk:** Re-paste during active HC triggers may re-arm handoffs — prefer DEV proof first; use `testMode: true` until Mike approves live sends.

---

## Production promotion (after DEV sign-off)

1. Repeat paste on Production **071**.
2. One controlled Production smoke with `testMode: true` (Schmidt HC).
3. Update `CHANGELOG.md` ### Airtable with paste date.
4. Set this doc **Status** → `Promoted to Production`.
