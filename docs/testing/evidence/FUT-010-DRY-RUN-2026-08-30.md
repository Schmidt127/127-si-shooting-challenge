# FUT-010 — Production dry-run evidence (2026-08-30)

**Backlog:** MRW-C10, FUT-010  
**Authority:** [`docs/deploy-checklists/FUT-010-intake-attachment-cleanup.md`](../../deploy-checklists/FUT-010-intake-attachment-cleanup.md)  
**Branch:** `cursor/mrw-c10-fut-010-dryrun-a2de`  
**Mode:** Read-only Production API + dry-run (no `--confirm-delete`, no writes)

---

## Offline tests (pre-flight gate)

| Suite | Command | Result |
|-------|---------|--------|
| Node contract | `node lib/intake-attachment-cleanup/intake-attachment-cleanup.test.js` | **PASS** — 15/15 |
| Python CLI/helpers | `cd tools/airtable && python3 -m unittest tests.test_fut_010_intake_attachment_cleanup` | **PASS** — 19/19 |

---

## Production preflight

**Command:** `python3 fut_010_intake_attachment_cleanup.py preflight`  
**Base:** `appn84sqPw03zEbTT` (Production)  
**Result:** `ready: true` — 13/13 required fields present, none missing.

### formulaPipelineSafety (summary)

| Field / guard | Depends on attachment? | Expected after clear | Reprocessing risk |
|---------------|------------------------|----------------------|-------------------|
| Upload Ready? | Yes | `0` | Low — 070a/b use Canonical URL + Storage Key duplicate guard |
| Ready to Send to Make? | Yes | Missing attachment message | Low — FUT-010 requires trigger unchecked |
| Why Not Ready for Make? | Yes | Informational missing-attachment | Low |
| Workflow Next Step | Yes | Missing Airtable Attachment | Low |
| Writeback Complete? | No | Unchanged when S3 fields populated | None |
| Upload Status / Canonical URL / Storage Key / Reviewer File URL | No | Unchanged | None |

**Reprocessing guards confirmed in repo:** 070a/070b `alreadyUploadedCanonical`, FUT-010 requires Send to Make Trigger unchecked, 022 triggers on Upload Status not attachment, 070c writeback-only.

**Mike attestation required:** `mikeAttestationRequired: true` — Production formulas/views/interfaces must tolerate empty attachment on Uploaded rows, or formulas patched first.

---

## Production dry-run (`--limit 50`)

**Command:** `python3 fut_010_intake_attachment_cleanup.py dry-run --limit 50 --output _preview/fut-010-dry-run-2026-08-30.json`  
**Generated:** 2026-08-30T15:06:52Z  
**Artifact:** [`tools/airtable/_preview/fut-010-dry-run-2026-08-30.json`](../../../tools/airtable/_preview/fut-010-dry-run-2026-08-30.json)

| Count | Value |
|-------|------:|
| Records scanned | 50 |
| **Eligible (would clear attachment)** | **0** |
| Skipped — verification failed | 27 |
| Skipped — ineligible | 23 |

### Verification-failure breakdown (27)

| Failure reason | Count |
|----------------|------:|
| Storage Key format invalid (must start with `shooting-challenge/`) | 20 |
| Canonical File URL missing or not HTTPS | 7 |

### Ineligible breakdown (23)

| Failure reason | Count |
|----------------|------:|
| Unsupported upload destination (Ignore / non-route) | 16 |
| Upload Status is not Uploaded | 7 |

### Sample record IDs (Production)

**Reconcile-relevant (Uploaded + attachment retained) — all failed verification:**

| Record ID | Category | Failure |
|-----------|----------|---------|
| `recMJ4jMDzHI6ikwt` | video | Storage Key format invalid |
| `rec94yqw5w7tqtJgc` | homework | Storage Key format invalid |
| `recDM9sAvqiFAhWy3` | homework | Storage Key format invalid |
| `rec2iGqWHCKy4ZRMJ` | video | Storage Key format invalid |

**Legacy-path rows (missing canonical URL):**

| Record ID | Category | Failure |
|-----------|----------|---------|
| `rec0K7T0dEYVoTk5V` | video | Canonical File URL missing or not HTTPS |
| `rec69s7jFeVPok4HJ` | homework | Canonical File URL missing or not HTTPS |

**Ineligible samples:**

| Record ID | Failure |
|-----------|---------|
| `recE3Ve9eo5pQ0SDT` | Unsupported upload destination |
| `recQAsg9PGqRaRRvd` | Upload Status is not Uploaded |

---

## Production reconcile (`--limit 100`, reconcile filter)

**Command:** `python3 fut_010_intake_attachment_cleanup.py reconcile --limit 100 --output _preview/fut-010-reconcile-2026-08-30.json`  
**Generated:** 2026-08-30T15:06:59Z  
**Artifact:** [`tools/airtable/_preview/fut-010-reconcile-2026-08-30.json`](../../../tools/airtable/_preview/fut-010-reconcile-2026-08-30.json)

| Count | Value |
|-------|------:|
| Reconcile candidates scanned | 24 |
| **Eligible** | **0** |
| Skipped — verification failed | 24 |

All 24 reconcile candidates failed with **Storage Key format invalid** — keys use enrollment-slug prefixes (e.g. `Schmidt_Barbie/Shooting_Challenge_2026-2027/...`) rather than the required `shooting-challenge/` prefix. Fail-closed behavior is working as designed; no attachment clears would have been attempted.

---

## AWS / S3 probe status

| Credential | Present in agent env? |
|------------|---------------------|
| `AIRTABLE_API_TOKEN` | Yes |
| `AWS_ACCESS_KEY_ID` | **No** |

**Impact:** No S3 `HeadObject` probes reached in this run — verification failed earlier on Storage Key format or missing Canonical URL. Before any pilot apply, Mike should re-run dry-run/reconcile with read-only AWS credentials so eligible rows (if any appear after key normalization or on canonical-path uploads) get full S3 + URL verification.

---

## Interpretation

1. **Dry-run harness succeeded** against live Production (read-only).
2. **Zero eligible rows** in both scans — fail-closed gates blocked every candidate.
3. Primary blockers: legacy Storage Key format (24 reconcile rows) and missing Canonical File URL on older `shooting-challenge/2025-2026/...` test rows.
4. **No destructive operations** were attempted or authorized.

---

## Mike sign-off checklist (Phase 1 — unchecked)

- [ ] Reviewed preflight `formulaPipelineSafety` and signed formula attestation
- [ ] Reviewed dry-run JSON: eligible / skipped / verification-failure counts
- [ ] Spot-checked 2 homework + 2 video rows (S3 object, Reviewer URL, attachment still present)
- [ ] Confirmed AWS credentials available for S3 HeadObject before pilot apply
- [ ] **Mike signs off on dry-run report** (date + record count): _______________

**Next step (Mike-only):** After attestation + AWS creds, optionally re-run dry-run; if eligible rows appear, proceed to Phase 2 single-record pilot per deploy checklist. **Do not run `apply --confirm-delete` without explicit approval.**
