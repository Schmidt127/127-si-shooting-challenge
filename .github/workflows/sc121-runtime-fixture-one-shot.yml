name: SC-121 runtime fixture one-shot

on:
  push:
    branches: ["reliability-wave/sc-121-terminal-week-boundary"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reliability-wave/sc-121-terminal-week-boundary
          fetch-depth: 0
      - name: Align 057 runtime Week fixture
        run: python3 tools/testing/sc121_runtime_fixture_patch.py
      - name: Commit and remove one-shot helpers
        run: |
          rm -f .github/workflows/sc121-runtime-fixture-one-shot.yml tools/testing/sc121_runtime_fixture_patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test(sc-121): align 057 runtime Week fixture"
          git push origin HEAD:reliability-wave/sc-121-terminal-week-boundary
